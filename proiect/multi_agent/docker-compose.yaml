services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: skillab_hw3_db
    environment:
      POSTGRES_USER: demo
      POSTGRES_PASSWORD: demo123
      POSTGRES_DB: rag_demo
    ports:
      - "5435:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
