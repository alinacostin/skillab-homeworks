# Database
sqlalchemy>=2.0
psycopg2-binary
alembic
pgvector

# Data processing
pandas
openpyxl
python-docx

# ML / Embeddings
sentence-transformers

# LLM (unul din acestea)
openai
google-generativeai
anthropic

# Utils
python-dotenv
tqdm
pydantic>=2.0
langgraph
sqlparse       # NL2SQL validate_sql (lipsea din scaffold-ul original)

# MCP (Tema 5 — server care expune agenții ca tool-uri)
fastmcp>=3.0    # aduce și pachetul `mcp` (client + tipuri JSON-RPC)
